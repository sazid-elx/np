 
<!-- Footer Popup Modal --> 
  <div class="popup-wrapper-footer" id="menuModal">
       <div class="popup-overlay"></div>
       <div class="popup-container">
         <div class="popup-header">
           <h3>Help<h3>
           <a class="close-popup" href="javascript:void(0)"> &#10006;</a>
         </div>
         <div class="popup-content">
           
            <div id="vertical_tab_nav">

              <ul class="tabsModal">
               {% for block in section.blocks %}
                  {% for link in linklists[block.settings.menu].links %}
                    {%- if link.url contains 'modal_' -%}
                    {%- assign page_handle = link.url | split: 'pages/' -%}
                     {%- assign page_handle_url = link.url | split: 'modal_' -%}
                    {%- assign include_page = pages[page_handle.last] -%}
                      
							<li><a rel="{{ page_handle_url.last }}">{{ include_page.title }}</a></li>
                         
                    {%- endif -%}
                  {% endfor %}
                {% endfor %} 
                	{% for link in linklists[section.settings.copyright-menu].links %}
                      {%- if link.url contains 'modal_' -%}
                      {%- assign page_handle = link.url | split: 'pages/' -%}
                       {%- assign page_handle_url = link.url | split: 'modal_' -%}
                      {%- assign include_page = pages[page_handle.last] -%}
                       		<li><a rel="{{ page_handle_url.last }}">{{ include_page.title }}</a></li>
                      {%- endif -%}
                    {% endfor %}
                	
                	{% for link in linklists[section.settings.modal-menu].links %}
                        {%- if link.url contains 'modal_' -%}
                        {%- assign page_handle = link.url | split: 'pages/' -%}
                         {%- assign page_handle_url = link.url | split: 'modal_' -%}
                        {%- assign include_page = pages[page_handle.last] -%}
                          	<li><a rel="{{ page_handle_url.last }}">{{ include_page.title }}</a></li>
                        {%- endif -%}
                      {% endfor %}
              </ul>
				<div class="tab_container">
                  {% for block in section.blocks %}
                  {% for link in linklists[block.settings.menu].links %}
                    {%- if link.url contains 'modal_' -%}
                    {%- assign page_handle = link.url | split: 'pages/' -%}
                     {%- assign page_handle_url = link.url | split: 'modal_' -%}
                    {%- assign include_page = pages[page_handle.last] -%}
                  	<article id="{{ page_handle_url.last }}">
                      <h3 class="footer-popop-title">{{ include_page.title }}:</h3>
                      <p>{{ include_page.content }}</p>
                    </article>
                  {%- endif -%}
                  {% endfor %}
                {% endfor %} 
                  
                  {% for link in linklists[section.settings.copyright-menu].links %}
                      {%- if link.url contains 'modal_' -%}
                      {%- assign page_handle = link.url | split: 'pages/' -%}
                       {%- assign page_handle_url = link.url | split: 'modal_' -%}
                      {%- assign include_page = pages[page_handle.last] -%}
                        <article id="{{ page_handle_url.last }}">
                            <h3 class="footer-popop-title">{{ include_page.title }}:</h3>
                            <p>{{ include_page.content }}</p>
                          </article>
                      {%- endif -%}
                    {% endfor %}
                  {% for link in linklists[section.settings.modal-menu].links %}
                      {%- if link.url contains 'modal_' -%}
                      {%- assign page_handle = link.url | split: 'pages/' -%}
                       {%- assign page_handle_url = link.url | split: 'modal_' -%}
                      {%- assign include_page = pages[page_handle.last] -%}
                        <article id="{{ page_handle_url.last }}">
                            <h3 class="footer-popop-title">{{ include_page.title }}:</h3>
                            <p>{{ include_page.content }}</p>
                          </article>
                      {%- endif -%}
                    {% endfor %}
              </div>
         </div>
       </div>
     </div>
</div>
  <!-- End of Footer Popup Modal here --> 

<style>
  
   /*   Modal popup   */
  .popupModal span {
    display: inline-block;
    cursor: pointer;

  }

  body, html {
    height: 100%
  }

  body.overlay, #menuModal {
    overflow: hidden
  }

  body.overlay .popup-wrapper-footer {
    display: grid
  }

  .footer-popop-title {
    font-size: 30px;
    color: #2B2725;
    font-family: "Silvana";
  }

  .popup-wrapper-footer {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 100000;
    display: none;
    place-items: center
  }

  #menuModal .popup-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, .6)
  }
  #menuModal .popup-header {
    min-height: 60px;
    border-bottom: 1px solid #eaeaea;
    display: flex;
    flex-direction: row;
    padding: 20px 10px;
    margin-left: 10px;
    margin-right: 10px;
  }
  #menuModal .popup-header h3 {
    margin-bottom: 0px;
	text-transform: capitalize;
    font-size: 18px;
	font-weight: 500;
    font-family: "Silvana";
  }

  #menuModal .popup-container {
    width: 90%;
    display: flex;
    flex-direction: column;
    max-width: 750px;
    background: #fff;
    position: absolute;
    z-index: 1;
    border-radius: 5px;
    box-shadow: 0 5px 20px rgba(0, 0, 0, .5);
    height: 60vh;
    top:50%;
	left:50%;
	transform:translate(-50%, -50%);
  }


  #menuModal .close-popup {
    color: #777;
    font-size: 1.2rem;
    right: 20px;
	position: absolute;
	top: 20px;

  }

  #menuModal .close-popup:active, #menuModal .close-popup:focus, #menuModal .close-popup:hover {
    color: #000;
    text-decoration: none;
  }


  #menuModal .popup-content {
    text-align: center;
    padding: 0rem 0rem 2rem;
    flex: auto;
    overflow: auto;
    padding-left: 0px;
    height: auto;
  }


  .popup-content .image {

  }
  /* End of modal popup */

  @media all and (max-width: 768px) {
   
    #menuModal .popup-container { 
    height: 80vh;
      width: 95%;
    }
  }
  
  
/*---------- vertical tab nav */

#menuModal #vertical_tab_nav {
  display: flex;
  width: 100%;
  justify-content: space-between;
}

#menuModal .tabsModal {
  float: left;
  margin: 0px;
  padding: 0px;
  list-style: none;
  overflow: hidden;
  width: 27%;
}

#menuModal .tabsModal li {
  border-bottom: 1px solid #eaeaea;
  margin-bottom: 1px;
  text-align: left;
  padding: 0px;
  margin-right: 10px;
}

#menuModal #vertical_tab_nav li:last-child {
  margin-bottom: 0px;
  border-bottom: 0px;
}

#menuModal #vertical_tab_nav li a {
  display: block;
  font-size: 15px;
  color: #000;
  text-decoration: none;
  padding: 10px 15px;
  text-align: center;
  cursor: pointer;
}
  
#menuModal #vertical_tab_nav li a:hover {
  
  color: rgba(0,0,0,0.45);
  }  
#menuModal .tab_container > h3 span {
    float: right;
	font-size: 15px;
  
  }
#menuModal .tab_container > h3 span.np-plus {
    float: right;
    background-image: url(https://www.ninetypercent.com/assets/icons/expand-0c90147d847d58b770c139433861d4902116c004da01341b37f3cf59a59267e0.svg);
    width: 20px;
    height: 20px;
    content: '';
    right: 16px;
    background-repeat: no-repeat;

  }
#menuModal .tab_container > h3 span.np-minus {
  	float: right;
    background-image: url(https://www.ninetypercent.com/assets/icons/expanded-5f6b532ba8f1f0dba47ead83aed85f1e675fa74e99a8dcf1f470ddd217154cb7.svg);
	width: 20px;
	height: 20px;
	content: '';
	right: 16px;
    background-repeat: no-repeat;
  
  }
#menuModal #vertical_tab_nav li a.selected {
  color: #000;
  border-bottom: 1px solid #000;
}

#menuModal .tab_container {
  display: block;
  background: #fff;
  width: 80%;
  position: sticky;
  position: -webkit-sticky;
overflow: auto;
}

#menuModal #vertical_tab_nav div article {
  display: none;
  margin: 0px;
  color: #000;
  padding: 10px 30px;
  text-align: left;
}

#menuModal #vertical_tab_nav div article p {
  margin: 0px 0px 20px 0px;
}

@media screen and (max-width: 768px) {
  #menuModal .tab_container {
    display: block;
    margin: 0 auto;
    width: 95%;
    border-top: none;
    border-radius: 0;
  }
  #menuModal .d_actived {
    background: #fff;
  }
/*   #menuModal .tab_container h3 {
  	padding: 15px 10px;
	border-bottom: 1px solid #eaeaea;
    cursor: pointer;
    font-size: 20px;
  } */
 #menuModal h3.tab_container:after {
  	 display:inline-block;
      width:50px;
      height:50px;
      background: linear-gradient(#fff,#fff),
        linear-gradient(#fff,#fff),
        #000;
      background-position:center;
      background-size: 50% 2px,2px 50%; /*thickness = 2px, length = 50% (25px)*/
      background-repeat:no-repeat;
  }
}


// style accordion links
#menuModal .tab_drawer_heading_modal {
    margin: 0;
    font-size: 14pt;
    color: #fff;
    text-decoration: none;
    padding: 3%;
    background: #57cac9;
    background: -webkit-linear-gradient(top, #444, #333);
    background: -moz-linear-gradient(top, #444, #333);
}

@media screen and (min-width: 769px) {
  #menuModal .tab_container {
        float: left;
        border-radius: 0 10px 10px 0;
        width: 100%;
    }
	#menuModal .tabsModal {
        display: none;
        overflow: auto;
        height: 50vh;
        position: sticky;
      	position: -webkit-sticky;
      	top: 0;
    }
  #menuModal .tab_drawer_heading_modal {
        display: none;
    }
   #menuModal article {
        padding: 0;
    }
}
  @media screen and (max-width: 768px) { 
  
    #menuModal .tabsModal {
    
    	display: none;
    }
    
    
  
  }

  
  
</style>

<script>
  
  (function atPollForjQuery() {
    if (window.jQuery !== undefined) {
        var $ = window.jQuery;
        if ($("#menuModal").length > 0) {
            try {

              $('#vertical_tab_nav > ul > li > a').eq(0).addClass("selected");
              $('#vertical_tab_nav > div > article').eq(0).css('display', 'block');
              var selIcon;
				
          $('#vertical_tab_nav > ul > li').on("click", function(e) {
			
            if ($(e.target).is("a")) {
				 
              $('#vertical_tab_nav > ul > li > a').removeClass("selected");
              $(e.target).addClass("selected");
				var int = $(this).index();
              var clicked_index = $("a", this).index(e.target);
             
              $('#vertical_tab_nav > div > article').css('display', 'none');
              
              $('#vertical_tab_nav > div > article').eq(int).fadeIn();
              $(".tab_drawer_heading_modal span").removeClass("np-minus").addClass("np-plus");
				$(".tab_drawer_heading_modal span").eq(int).removeClass("np-plus").addClass("np-minus");
              $(".tab_drawer_heading_modal").eq(int).addClass("d_actived")
            }
            selIcon = $("a", this).attr('rel');
             $(this).blur();
    			return false;
            
          });
              $(".tab_drawer_heading_modal span").addClass("np-plus");
              
            $(".tab_drawer_heading_modal").on("click", function() {
				$("article").hide();
              
              var d_activeTab = $(this).attr("rel"); 
              
              
              
              if($(this).hasClass("d_actived")){
              	console.log('activate');
                $(".tab_drawer_heading_modal").removeClass("d_actived");
                 $(".tab_drawer_heading_modal span").removeClass("np-minus").addClass("np-plus");
                $("article").hide();
              }
              else{
                $("#"+d_activeTab).fadeIn();
              	
              $(this).addClass("d_actived");
				$(".tab_drawer_heading_modal span").removeClass("np-minus").addClass("np-plus");
                $(this).find("span").removeClass("np-plus").addClass("np-minus");
              $("ul.tabsModal li a").removeClass("selected");
              $("ul.tabsModal li a[rel^='"+d_activeTab+"']").addClass("selected");
              }
            });
              
             
				
            } catch (err) {
                console.log('TRY ERROR: ' + err);
            }   
          
        } else {
            setTimeout(atPollForjQuery, 25);
        }
    } else {
        setTimeout(atPollForjQuery, 25);
    }
})();
  


</script>